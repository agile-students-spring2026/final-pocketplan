The front-end of your project lives in this directory (create-react-app).

To install and run:
  npm install
  npm start

Build output: npm run build → build/

See CRA-README.md for create-react-app defaults.
